# Вхід за PIN

Зроблено так само, як у проєкті ЗІП (`Spare-parts-mechanic-service`), щоб два
застосунки поводилися однаково і механік не вчився двом різним звичкам.

## Коротко

Механік відкриває застосунок і вводить свій код. Прізвище зі списку більше не
обирається — його дає вхід, і саме воно стоїть у звіті.

**PIN живе тільки в кадровій таблиці, колонка Q.** Змінити його з застосунку
не можна — ані собі, ані комусь іншому. Забув, скомпрометований, збігся
з чужим — правиться в таблиці, більше ніде.

Копії доступів у робочій таблиці немає: ролі й PIN перечитуються з кадрової
при кожному вході **і перед кожним записом звіту**. Тому звільнення або зміна
ролі діють негайно, а не після того, як скінчиться сесія.

---

## Обов'язкова умова: PIN мають бути унікальними

Логін — це сам PIN, без вибору прізвища. Якщо той самий код стоїть у двох
працівників, вхід **відхиляється для обох** із поясненням: інакше звіт пішов
би від імені випадкової людини.

Станом на 24.08.2026 у кадровій:

| PIN | Хто має доступ до чек-листа | Наслідок |
|---|---|---|
| `1111` | Гончарук Ольга Михайлівна (`shift.master`), Гора Андрій Олександрович (`mech.use`), Шута Олександра Сергіівна (`shift.master`) | **не увійде жоден із трьох** |
| `6699` | Галагін Євгеній Ярославович (`mech.admin`) | вхід працює |
| `3773` | Сабадаш Геннадій Петрович (`mech.use`) | вхід працює |
| `8899` | Диндар Анастасія (`admin`) | вхід працює |
| `9988` | Бузницький Юрій (`admin`) | вхід працює |

`1111` — це стартовий пароль, який стоїть у 25 працівників. **Перед запуском
трьом треба призначити власні PIN у колонці Q.** Той самий крок уже потрібен
і для ЗІП — там ті самі люди й та сама причина.

Перевірити стан: **`auditPins()`** з редактора Apps Script. Друкує, хто має
доступ, де дублікати й у кого порожній email. Повні PIN не показує — тільки
першу цифру (`1***`).

---

## Що дає роль

| роль | здає чек-лист механіка | здає чек-лист майстра | отримує звіти механіків | отримує звіти майстрів |
|---|:--:|:--:|:--:|:--:|
| `mech.use` | так | — | — | — |
| `mech.admin` | так | — | так | — |
| `shift.master` | — | так | — | так |
| `admin` | так | так | так | так |

Джерело правди — константа `ROLE_PERMISSIONS` в `Auth.gs`; її ж використовує
клієнт, щоб вирішити, який чек-лист показати.

Ролі беруться так само, як у ЗІП: якщо заповнена колонка S «ролі відібрані» —
береться вона; інакше ролі посади (`_REF_Positions`, колонка D) плюс колонка R
«ролі додатково». Роздільник — кома, крапка з комою або пробіл. Ролі інших
систем (`zip.*`, `qc.*`, `supply.*`) до чек-листа не пускають.

### Кому йде звіт

Списку одержувачів у налаштуваннях **немає** — він випливає з ролей і колонки O
кадрової, як і все інше в цьому проєкті:

    звіт механіка → усі, хто має reportMech   (mech.admin, admin)
    звіт майстра  → усі, хто має reportMaster (shift.master, admin)

Станом на 25.08.2026:

| звіт | кому |
|---|---|
| механіка | Buznitskiy7@, dyndarnastia@, evgeniy.galagin@ |
| майстра | Buznitskiy7@, dyndarnastia@, olgagoncharukm@, sashaalieva18@ |

Нова людина з потрібною роллю починає отримувати звіти одразу, звільнена —
одразу перестає. Правити властивості скрипта не треба, і два списки не
розʼїжджаються між собою.

`MAIL_TO` лишається **запасним варіантом**: спрацьовує, тільки якщо кадрова
недоступна або в нікого з потрібною роллю не заповнений email — щоб звіт
пішов хоч кудись, а не в нікуди. Кожен такий випадок пишеться в журнал подій.

Перевірити, хто що отримає: **`auditRecipients()`** з `Report.gs`. Нічого
не надсилає, друкує обидва списки і показує, з якої ролі кожна адреса взялася.

### Тема листа

    🔧 Звіт (Механік): Початок зміни - Прізвище
    📋 Звіт (Майстер): Кінець зміни - Прізвище
    ❗ 🔧 Звіт (Механік): …     — є відхилення
    ⚠️ 🔧 Звіт (Механік): …     — є попередження
    🔔 Чек-лист за 2026-08-25: є питання      — щоденний контроль
    📊 Чек-лист: відхилення за тиждень        — тижневий дайджест

Позначка стану в темі — щоб зміну з відхиленнями було видно **не відкриваючи
листа**. Саме через однакові теми 303 сигнали за пів року лежали непоміченими.
Побічний виграш: чисті звіти й проблемні потрапляють у різні ланцюжки Gmail.

Заголовок **усередині** листа лишився без піктограм — верстку не змінено.

### Від кого приходить лист

`MailApp` надсилає від імені того акаунта, **під яким виконується скрипт**, —
тобто власника розгортання («Виконувати від імені»). Тому листи зі старих
розгортань приходять від `engineeringflp17`, а з нового — від того, хто його
створив. Коли всі телефони перейдуть на нову адресу і старі розгортання
буде заархівовано, відправник стане один.

Якщо в когось порожній email у колонці O, `auditPins()` попереджає про це
щоразу. Заповнити можна будь-коли, код чіпати не треба.

---

## Що перетинає мережу

| Напрямок | Вміст |
|---|---|
| Клієнт → сервер | `{action:'login', pin, deviceId}` — один раз під час входу |
| Сервер → клієнт | ім'я, `user_id`, ролі, права, токен, строк дії |
| **Ніколи не передається** | PIN у відповіді, список працівників, чужі ролі й статуси |

Сервер відповідає лише про того, чий PIN збігся. Скільки в компанії людей і
які в них коди — з застосунку не видно.

Автора звіту клієнт не надсилає взагалі: ні `user_name`, ні `user_id` у payload
немає — сервер бере ім'я з підтвердженої сесії. Підписати звіт чужим прізвищем,
підправивши запит, неможливо.

Через це звіт, складений у чергу без зв'язку, має локальну позначку, хто його
створив (на сервер вона не йде). Якщо на тому самому телефоні встигне увійти
інша людина, черга **не відправиться** — інакше сервер підписав би звіт нею.
Застосунок скаже, що чекає чужий звіт, і відправить його, коли автор увійде.

---

## Токен

```
base64url({ id: emp_id, e: час_завершення, d: deviceId }) + "." + HMAC-SHA256(тіло, секрет)
```

Секрет створюється сам при першому запуску і лежить у властивостях скрипта
(`AUTH_SECRET`) — не в коді й не в таблиці. Токен не містить ні PIN, ні ролей:
ролі щоразу перечитуються з кадрової.

Токен виданий конкретному пристрою. Скопіювати його на інший телефон
не вийде — відсутність або розбіжність `deviceId` вважається підробкою.

Строк перевіряє сервер за значенням усередині підписаного токена, тож
переведення годинника на телефоні нічого не дає.

---

## Сесія на пристрої

| Ключ у `localStorage` | Вміст | Живе |
|---|---|---|
| `checklistSessionV1` | токен, ім'я, ролі, права, строк | 12 годин — одна зміна |
| `offlineReportsQueue` | звіти, що чекають на зв'язок | до відправлення |
| `checklistDeviceIdV1` | ідентифікатор пристрою | постійно |
| `checklistLastActivityV1` | мітка останньої дії | для автовиходу |
| `checklistOfflineUnlockV1` | перевірник PIN для входу без мережі | 7 діб |

**PIN не зберігається ніде.**

**Автовихід через 10 хвилин бездіяльності** — те саме число, що в ЗІП.
Дотик, клік, набір і прокрутка перезапускають таймер. Мітка часу лежить
у сховищі, тому простій переживає і перезавантаження сторінки, і закриття
вкладки; повернення на вкладку (`visibilitychange`) теж перевіряється.
Телефон, що пролежав у роздягальні, при відкритті одразу просить PIN.

Якщо обхід цеху з довгими паузами між пунктами впиратиметься в цей ліміт —
змінюється однією константою `IDLE_LIMIT_MS`. Заповнене при цьому не
втрачається: чек-лист лежить окремо від сесії.

**Вхід без мережі.** Після успішного входу онлайн на пристрої лишається
перевірник: сіль і PBKDF2-SHA256 від PIN зі 150 000 ітерацій, плюс ім'я
й права. Самого PIN там немає. Діє 7 діб від останнього входу онлайн
і відкриває сесію на 12 годин, у якій звіт **тільки лягає в чергу** —
відправити його без справжнього токена не можна.

Коли зв'язок повертається, черга йде на сервер із поточним токеном. Якщо
сесії немає, звіти **залишаються в черзі**, а застосунок просить увійти —
нічого не губиться.

---

## Захист від перебору

* 5 невдалих спроб з одного пристрою → пауза 5 хвилин, причому блокується
  навіть правильний PIN;
* кожна невдала спроба сповільнюється на 400 мс;
* вхід шукає збіг лише серед тих, хто має доступ до чек-листа — PIN решти
  персоналу не підходить у принципі;
* повний PIN ніде не логується: у журнал іде тільки його довжина й номер спроби.

Зловмисник може міняти `deviceId`, тож це сповільнення, а не бар'єр.
Справжній захист — довші й унікальні PIN.

---

## Порядок запуску

1. **`renameMasterItems()`** — один раз, переводить пункти майстра на ід
   живого фронтенду.
2. **`authSelfTest()`** — підпис токена, прострочення, підробка, прив'язка
   до пристрою, розбір ролей. Нічого не записує.
3. **`auditPins()`** — хто має доступ і де дублікати.
   *Перший запуск попросить дозвіл на читання кадрової таблиці.*
   Якщо поруч із кимось стоїть «⚠ немає в 03_Працівники» — запустіть
   **`addMissingEmployees()`**: без рядка в довіднику його звіти лягають
   на `U-000` «особу не встановлено» і в 12_Відповідях їх ні від кого
   не відрізнити.
4. Призначити унікальні PIN трьом працівникам у колонці Q кадрової.
5. `auditPins()` ще раз — має бути «✅ Усі PIN унікальні».
6. Розгорнути новий `index.html` і нову версію скрипта
   (Розгортання → Керувати розгортаннями → олівець → Нова версія
   на **тих самих** розгортаннях, і на механіковому, і на майстровому —
   інакше застосунок і далі говоритиме зі старим бекендом).
7. Коли всі перейдуть на новий застосунок — властивість скрипта
   **`AUTH_REQUIRED = так`**. До того звіт без входу приймається: інакше
   в день розгортання звіти зі старих телефонів почали б губитися.

Після змін у кадровій робити нічого не треба — вони діють одразу.

---

## Що робити, коли

**Механік забув PIN або треба його змінити.** Колонка Q кадрової таблиці.
Застосунок підхопить при наступному вході; стара сесія на телефоні доживе
до 12 годин, після чого попросить новий код.

**Треба закрити доступ негайно.** Статус `fired` у колонці H кадрової або
прибрати роль. Діє з наступного ж запиту — навіть посеред відкритої сесії.

**Прийшов новий механік.** Роль у кадровій і унікальний PIN у колонці Q —
цього досить, щоб він увійшов. Щоб його звіти не лягали на `U-000`,
запустіть `addMissingEmployees()`.

**Пише «Немає зв'язку із сервером» при вході.** Розгортання має бути
доступним «Усім» — інакше браузер не може прочитати відповідь.

**«Немає доступу до кадрової таблиці».** Скрипт виконується від імені
акаунта, вказаного в розгортанні як «Виконувати від імені», — а це не
обов'язково власник таблиці. Дайте цьому акаунту право **«Читач»** на
кадрову таблицю. Чий саме це акаунт — покаже `preflight()` або
`checkPhotoFolder()`.

Так сталося 02.09.2026: акаунт розгортання `engineeringflp17@gmail.com`
втратив доступ до кадрової, і **жоден вхід не проходив**. У застосунку це
виглядало як «немає мережі», бо помилка вилітала з `doPost` і Google віддавав
сторінку помилки замість JSON. Тепер такий стан застосунок називає прямо:
«Довідник працівників недоступний… Це не ваш PIN».

Перевіряти цей стан варто першим: **`preflight()`** має показати
«✅ Кадрова таблиця: доступ до чек-листа мають N працівників».
Якщо там ❌ — усе інше не має значення.

---

## Чек-лист майстра

Живе окремо: **`YuriyBuz/shift-checklist`**, файл `check-list-master.html`,
опубліковано на `yuriybuz.github.io/shift-checklist`. Гілка зі змінами —
`claude/master-checklist-auth`.

Той самий вхід за PIN, ті самі правила. Роль `mech.use` застосунок майстра
не пускає, роль `shift.master` не пускає застосунок механіка; `admin` заходить
в обидва. Сесія, автовихід і офлайн-вхід — так само.

**Ід пунктів звірено з живим фронтендом.** Коли структуру майстра відновлювали
з даних, застосунку не було під рукою, і пункти отримали власні ід
(`master.m1-1`). Насправді фронтенд називає їх `s1-1`, `e3-2`. Усі 30 збіглися
за текстом, стадією і складом груп, тож довідник переїхав на ід клієнта.
Перенести це на живу таблицю треба **один раз**:

    renameMasterItems()

Функція міняє ід одразу в чотирьох місцях — `01_Пункти` (разом із `group_id`),
`02_Варіанти`, `12_Відповіді`, `13_Фото` — інакше вже перенесені відповіді
відв'язалися б від довідника. Повторний запуск нічого не робить.

### Одержувачі звіту майстра змінюються

Старий застосунок майстра **сам диктував, кому надсилати**: у кожному звіті
їхав зашитий список

    Buznitskiy7@gmail.com, Dyndarnastia@gmail.com,
    olgagoncharukm@gmail.com, sashaalieva18@gmail.com

Тепер список визначає сервер за ролями в кадровій — і виходять рівно ті самі
чотири адреси: Гончарук і Шута мають `shift.master`, Диндар і Бузницький —
`admin`. Нічого вписувати руками не треба.

Перевірка — `auditRecipients()`.

---

## Два розгортання

Застосунки звертаються до **різних URL** одного проєкту Apps Script:

| застосунок | `/exec` |
|---|---|
| механік | `AKfycbz3kY2lMZLsgI0r06f7gP1Zbdqiy_DPc-S_y15W2rQrerLrPVFdhwVEk2iZJwGyG-mYbQ` |
| майстер | `AKfycbxN1HUmiD55ZuZ7xw1p_qSw0LMsv24mSQkGaN2frGaFCYSLqQYRE-AXyQyBF_A7Puyc6Q` |

Кожне розгортання віддає **свою заморожену версію** коду. Тому нову версію
треба випустити для обох — інакше один застосунок працюватиме з новим
бекендом, а другий і далі зі старим.

---

## Чого тут немає

* **Зміни PIN із застосунку** — навмисно. Кадрова таблиця лишається єдиним
  місцем, де живуть коди.
* **Аркуша доступів у робочій таблиці** — теж навмисно. Копія довідника
  застаріває; краще читати першоджерело.
* **Спільного коду двох застосунків.** Вхід, черга й відправка в них однакові
  рядок у рядок, але кожен — самодостатній HTML-файл в окремому репозиторії,
  і склеювати їх у бібліотеку зараз означало б зламати обидва PWA. Правку
  треба вносити двічі — це записано тут навмисно, щоб не забути.
